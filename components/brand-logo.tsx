/* oxlint-disable next/no-img-element -- Pre-sized static asset shared with the standalone public preview; no image optimizer required. */
/** Approved Shared Canopy identity, shared by public and member navigation. */
export function BrandLogo() {
  return (
    <span className="brand-logo">
      <img
        src="/brand/shared-canopy-logo-v1.png"
        alt="VergeCommon"
        width={1983}
        height={793}
      />
    </span>
  );
}
